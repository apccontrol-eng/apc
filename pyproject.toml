[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "apc"
version = "0.1.0"
description = "Advanced Process Control sandbox"
authors = [{ name = "Emil" }]
dependencies = [
    "numpy",
    "matplotlib"
]

[tool.setuptools.packages.find]
include = ["apc*"]